x-backend-build: &backend-build
  context: .
  dockerfile: Dockerfile

services:
  bootstrap:
    build: *backend-build

  api:
    build: *backend-build

  worker:
    build: *backend-build

  mcp:
    build: *backend-build

  ui:
    build:
      context: ./ui
      dockerfile: Dockerfile
